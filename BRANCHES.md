# Backend branches

- **main** — PostgreSQL (Npgsql), ASP.NET Core 8. See `README.md` and `../docs/AWS-HOSTING.md` for deployment.

## When switching branches

If you use multiple local branches with different `appsettings.json` contents, reset or stash config before switching so you do not point at the wrong database.
